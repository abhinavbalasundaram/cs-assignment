apiVersion: networking.gke.io/v1
kind: MultiClusterService
metadata:
  name: app-b-mcs
  namespace: web
spec:
  template:
    spec:
      selector:
        app: app-b
      ports:
        - name: http
          protocol: TCP
          port: 80
          targetPort: 8080